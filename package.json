{
  "name": "crm-rm-v2",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3000",
    "build": "next build",
    "start": "next start -p 3000",
    "lint": "next lint",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "db:generate": "prisma generate",
    "import": "node scripts/import-from-json.js",
    "proxy:setup": "echo 'scripts/setup-proxy.bat 을 관리자 권한으로 실행하세요'",
    "proxy:remove": "echo 'scripts/remove-proxy.bat 을 관리자 권한으로 실행하세요'"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "next": "^14.2.18",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
